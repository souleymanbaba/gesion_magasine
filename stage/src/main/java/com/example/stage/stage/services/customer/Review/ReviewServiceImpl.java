package com.example.stage.stage.services.customer.Review;

import com.example.stage.stage.repostory.OrderRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class ReviewServiceImpl implements ReviewService{
//    private final OrderRepository orderRepository;
//    no usages
//    public Ordered Products ResponseDto getürderedProductsDetailsByOrderId(Long orderId)(
//    Optional<Order> optionalOrder orderRepository.findById(orderId);
//    Ordered ProductsResponseDto ordered ProductsResponseDto
//new Ordered ProductsResponseDto()
//if(optionalOrder.isPresent()){
//        ordered ProductsResponseDto.setOrderAmount (optionalOrder.get().getAmount());
//        List<ProductDto> productDtoList = new ArrayList<>();
//        for (CartItems cartItems: optionalOrder.get().getCartItems()){
//            Product to productDto new ProductDto();
//            productDto.setId(cartItems.getProduct().getId());
//            productDto.setName(cartItems.getProduct().getName());
}
